# rosbag 回放自瞄练习

26 届 · 基于 TongjiSuperPower / sp_vision_25 的暑期练习（ROS2 + rosbag 回放版）

本轮练习由于暑假在家没有相机、下位机等硬件，所以**全程离线**：目前已用真车录了 **rosbag**（一段"逐帧图像 + 同源时刻云台姿态"的数据包），你要把开源自瞄项目 `sp_vision_25` 改造成 **ROS2 结构**，让它从这段 bag 里读数据，跑通完整自瞄链路，并在 PlotJuggler 里看到解算与跟踪结果。

**这次练习的核心目标：**

- **把 sp_vision 改造成 ROS2 包**，能 `colcon build`、`ros2 run` 起来。
- **从 rosbag 订阅输入**，把图像和姿态按时间戳配对，喂进原算法链路。
- **跑通检测 → 解算 → 跟踪**，并用 PlotJuggler 观察世界系目标是否稳定。

## 〇、三分钟速览（先看这里）

**做什么**：把 `sp_vision_25` 改成 ROS2 包 → 从所提供的 rosbag 订阅"图像 + 云台姿态" → 喂进它原有的检测/解算/跟踪算法 → 在 PlotJuggler 看结果。**算法本身不用你写，你做的是"换数据入口"。**

**怎么算完成**：按顺序打通 5 个里程碑（详见 §六），全部达标即合格：

| | 里程碑 | 验收 |
| --- | --- | --- |
| M1 | ROS 化编译 | `colcon build` 通过，能 `ros2 run` 起你的节点 |
| M2 | 回放订阅配对 | 播 bag 时节点能拿到配对好的 `(图像, 四元数, 时间戳)` |
| M3 | 检测出装甲板 | 可视化窗口稳定框住装甲板、分类正确 |
| M4 | 解算 + 跟踪 | 得到世界系目标位置/速度，tracker 状态正常切换 |
| M5 | PlotJuggler | 曲线连续合理，**运动云台下世界系目标不漂移** |

**最后交什么**（详见 §八）：一个 **公开 git 仓库**，里面包含 ——

1. 改造好、能编译能回放的 **完整代码**（sp_vision + 自建的 autoaim_msgs 包）；
2. 一份 **`FIX_RECORD.md`**（学习/开发记录，验收核心，从第一天就开始写，结构见 §4.3）；
3. **跑通证据**：编译成功截图、M3 检测框截图、M5 曲线截图、一段 10~30s 包含图像和曲线滚动的录屏。

**下面这份文档怎么读**：一~三 先读懂项目和数据 → 四 把仓库和笔记建起来 → 五 填内外参 → 六 按 M1–M5 推进 → 八 对照交付清单自检。**先通读一遍再动手。**

## 一、项目解释

`sp_vision_25` 是同济 SuperPower 战队 25 赛季的 RoboMaster 视觉自瞄框架。它原本是独立 CMake 程序，`src/standard.cpp` 是 `main`，输入来自 `io::Camera`（相机）和 `io::CBoard`（下位机）。看它的主循环就能看懂整条骨架：

```cpp
camera.read(img, t);              // 1. 取一帧图像 + 时间戳
q = cboard.imu_at(t - 1ms);       // 2. 取该时刻云台姿态四元数
solver.set_R_gimbal2world(q);     // 3. 用 q 建立 gimbal→world 变换
auto armors  = detector.detect(img);        // 4. 检测装甲板
auto targets = tracker.track(armors, t);    // 5. PnP 解算 + 跟踪 + 预测
auto command = aimer.aim(targets, t, ...);  // 6. 算云台指令
cboard.send(command);             // 7. 发给下位机
```

## 二、你会拿到什么

1. **`sp_vision_25-main`**：开源自瞄项目（C++17，含检测/解算/跟踪/预测算法）。
2. **rosbag**：所提供的 `move_translate_bag`——一段**运动云台 + 目标静止**的场景。
3. **相机内参 + 外参、消息定义**：做 PnP、坐标变换和订阅 `/imu/quaternion` 必需（见第五节，本文已附具体数值与定义）。

## 三、rosbag 数据契约（务必先理解）

bag 里只有两个话题，**逐帧一一对应**：

| 话题 | 类型 | 内容 |
| --- | --- | --- |
| `/image_raw` | `sensor_msgs/msg/Image` | BGR8，1280×1024，相机原始帧 |
| `/imu/quaternion` | `autoaim_msgs/msg/Orienta` | 该帧对应时刻的云台姿态四元数（字段 `w/x/y/z`；`dm_*` 恒为 0，忽略）|

关键规则：

- 同一帧的图像和四元数由录制端**同帧同序发出**，图像 msg 带 `header.stamp`（四元数 msg 无 header）。你订阅时要把它们**按时间/顺序配对**，组成 `(图像, 四元数, 时间戳)` 再送算法。
- `header.stamp` 是**单调时钟（steady_clock）的纳秒值**，不是日期。它的绝对值无意义，你只用它做两件事：① 图像↔四元数配对；② 算相邻帧时间差 `dt`（tracker/EKF 需要）。还原：`steady_clock::time_point(std::chrono::nanoseconds(stamp_ns))`。
- `/imu/quaternion` 对应 sp_vision 原来 `cboard.imu_at(t)` 返回的那个四元数 `q`。

> ⚠️ `autoaim_msgs/msg/Orienta` 是录制方自定义的消息类型。你的工程里**必须有一个能生成这个类型的消息包**，否则既编译不过、也无法反序列化 bag 里的 `/imu/quaternion`。这个包需要你自己建，定义见 §5.2。

## 四、仓库怎么管理

### 4.1 先搞清楚：ROS2 工作区是什么

**工作区（workspace）就是一个普通文件夹**，只是约定它下面有个 `src/` 子目录，用来放一个个"包（package）"。sp_vision 是一个包，`autoaim_msgs` 是另一个包，它们并排放在 `src/` 里。

编译命令 `colcon build` **必须在工作区根目录运行**（不是在某个包里面）。它会自动扫描 `src/` 下所有包一起编译，并在根目录生成三个目录：`build/`（编译中间产物）、`install/`（编译结果）、`log/`（日志）。这三个是自动生成的，不用手动建，也**不进 git**。

目标结构如下（这整个 `sp_vision_ws/` 就是你要提交的仓库）：

```
sp_vision_ws/              # 工作区根目录 = 你的 git 仓库（在这里跑 colcon build）
├── FIX_RECORD.md          # 你的学习笔记 / 开发记录（验收核心，见 §4.3）
├── shots/                 # 截图/录屏目录，FIX_RECORD.md 里引用它
├── .gitignore
├── src/                   # 你手动建，放所有包
│   ├── sp_vision/         # 原 sp_vision_25 内容放这里（一个包）
│   └── autoaim_msgs/      # 订阅 /imu/quaternion 所需的消息包（自己建，见 §5.2）
├── build/                 # colcon build 自动生成，不进 git
├── install/               # colcon build 自动生成，不进 git
└── log/                   # colcon build 自动生成，不进 git
```

### 4.2 建立仓库与 git 管理

**建议先把仓库骨架搭好，再往里填代码。** 一步步来：

```bash
# 1) 建工作区
mkdir -p ~/sp_vision_ws/src && cd ~/sp_vision_ws/src

# 2) 克隆开源仓库，并重命名为 sp_vision
git clone https://github.com/TongjiSuperPower/sp_vision_25.git sp_vision

# 3) 删掉它自带的 git 历史（它指向别人的仓库）！否则你在工作区建自己的仓库时会形成"嵌套仓库"，git 会犯迷糊、你的改动提交不进去。
rm -rf sp_vision/.git

# 4) 回到工作区根目录，初始化你自己的仓库
cd ~/sp_vision_ws
git init
git checkout -b feature/<你的名字>-ros-migration    # 建立自己的分支 例：feature/zhangsan-ros-migration
```

在工作区根目录建 `.gitignore`，至少包含：

```gitignore
# ROS2 自动生成目录
build/
install/
log/
# 大文件：数据包、视频、模型权重一律不进 git
bags/
*.db3
*.db3.zstd
*.mp4
*.avi
*.onnx
```

**git 管理要求：**

- **建一个公开仓库作为本次统一验收仓库**。
- **不要直接推 main**，在自己的分支 `feature/<你的名字>-ros-migration` 上开发。
- **鼓励按功能点多次 commit**，一个里程碑/一个功能一次，信息写清做了什么，例如 `feat: 新增 bag 订阅节点，跑通 M2 数据配对`、`fix: 修正四元数配对时序`。别把一堆改动堆成一个 commit。**每完成一个里程碑就 push 一次**，验收以截止时间前分支上的最新提交为准。
- **不要提交大文件**：rosbag（几 GB）、视频、模型权重进 git 会让仓库爆炸、GitHub 也会拒绝。靠上面的 `.gitignore` 挡住；提交前用 `git status` 确认没把 `bags/`、`build/`、`install/` 加进去。
- 代码和截图放进仓库，**所有文字说明统一写在 `FIX_RECORD.md` 里**（见 §4.3），不要散落在各处。

### 4.3 修改记录 FIX_RECORD.md（验收核心，从第一天就开始写）

在仓库根目录维护**一份** `FIX_RECORD.md`，它要能承担"说明文档"的作用：让没做过这个项目的人看了，也能知道你做到什么程度、怎么把项目跑起来、自瞄大概是怎么回事。**从 M1 就开始写，别等最后补。**

**固定结构（三块）：**

1. **仓库简介**：这是什么仓库、当前主要目录怎么分。
2. **里程碑记录**：每个里程碑一个二级标题（`## M1`…`## M5`），每块至少写清（具体里程碑详见第六部分）：
   - 本里程碑完成了什么、怎么验证的（配截图）；
   - 修改了什么；
   - 遇到的主要问题；
   - 你是**怎么排查、怎么解决**的；
   - 现在还没解决什么。
3. **项目说明**（做到 M3 后开始稳定维护）：至少写清 **项目功能 / 依赖 / 输入源 / 输出话题或结果 / 参数入口 / 当前已知局限**——让别人拿到你的仓库后，照着能把项目跑起来。

**表达要求：**

- 说明尽量写成完整句子，不要只列关键词；用规范的 markdown。
- **截图可以放，但不能替代文字说明。** 同一件事不要重复写很多遍，持续沿用的信息集中写在"项目说明"里。

**不希望出现的情况**：

- 只有截图，没有文字说明；只有感想，没有运行方法。
- 只写"已完成 / 已解决"，不写**怎么**完成、**怎么**解决。
- 项目已经能跑，但 `FIX_RECORD.md` 里看不出怎么复现。

## 五、提供的数据与配置

本节是随 bag 一起提供的全部数据信息：**相机内外参**（PnP 与坐标变换要用）和 **消息定义**（订阅 `/imu/quaternion` 要用）。

### 5.1 相机内外参（PnP 必需，用下面数值）

镜头是 **6mm**，把下面的值填进 sp_vision 的 `configs/standard3.yaml` 对应位置。

**内参 / 畸变（6mm 档，1280×1024 标定）：**

```
focal_length (fx, fy):     1291.2365779471, 1291.6532761007
principal_point (cx, cy):  622.5361271492, 497.3760733938
disto_param (k1,k2,p1,p2,k3): -0.0837411641, 0.1316900341, -0.0001941245, -0.0004098046, 0.0
```

**相机→云台外参 `rotation_matrix_camera_to_gimbal`（行主序 3×3）：**

```
 0.0248055224, -0.0172352660,  0.9995437117,
-0.9985846022, -0.0474822914,  0.0239629768,
 0.0470476175, -0.9987233739, -0.0183886942
```

**`t_camera_to_gimbal`（米）：** `[0.1640166852, 0.0527662433, 0.0192375745]`

**云台→IMU `rotation_matrix_gimbal_to_imu`（行主序 3×3）：**

```
 0,  1,  0,
-1,  0,  0,
 0,  0,  1
```

### 5.2 消息定义 autoaim_msgs（订阅 /imu/quaternion 必需）

`/imu/quaternion` 的类型是 `autoaim_msgs/msg/Orienta`。你的工程里必须有一个能生成这个类型的消息包，否则既编译不过、也无法反序列化 bag 里的 `/imu/quaternion`。这个包不大，**你在 `src/` 下自己建一个即可，包名、消息名、字段必须和下面完全一致**（否则和 bag 里的类型对不上，收不到消息）。

这里**只给出消息定义 `Orienta.msg`**，配套的 `package.xml` 和 `CMakeLists.txt` 按 ROS2 自定义消息包的标准写法自己搭。目标结构：

```
autoaim_msgs/
├── package.xml        # 自己写
├── CMakeLists.txt     # 自己写
└── msg/
    └── Orienta.msg    # 用下面给定的内容
```

**`msg/Orienta.msg`**（8 个 float32，必须和下面完全一致）：

```
float32 w
float32 x
float32 y
float32 z
float32 dm_w
float32 dm_x
float32 dm_y
float32 dm_z
```

`package.xml` / `CMakeLists.txt` 自己写时，注意消息包和普通节点包不同，容易漏的几点（漏了会编译失败或生成不出类型）：

- `Orienta.msg` 只含基础类型 `float32`，**不依赖 `std_msgs`/`geometry_msgs` 等其它消息包**，所以不用加跨包依赖。
- `package.xml` 里要有 `rosidl_default_generators`（build）、`rosidl_default_runtime`（exec），以及 `<member_of_group>rosidl_interface_packages</member_of_group>`。
- `CMakeLists.txt` 里要 `find_package(rosidl_default_generators REQUIRED)`，并用 `rosidl_generate_interfaces(${PROJECT_NAME} "msg/Orienta.msg")` 生成消息。

建好后在工作区根目录编译、验证：

```bash
cd ~/sp_vision_ws && colcon build --symlink-install
source install/setup.bash
ros2 interface show autoaim_msgs/msg/Orienta   # 能打印出 8 个 float32 字段即成功
```

之后 sp_vision 包要订阅 `/imu/quaternion`，在它的 `package.xml` 里加 `<depend>autoaim_msgs</depend>`、`CMakeLists.txt` 里 `find_package(autoaim_msgs REQUIRED)` 并 `ament_target_dependencies(<你的节点> ... autoaim_msgs)` 即可。

## 六、基础项（建议按顺序推进，逐一完成）

| 里程碑 | 目标 | 自检方法 |
| --- | --- | --- |
| **M1 ROS 化编译** | 把 sp_vision 改成 colcon 包，`colcon build` 通过 | build 无错，`ros2 pkg list \| grep sp_vision` 能看到，能 `ros2 run` 起节点 |
| **M2 回放订阅配对** | 节点订阅 bag，拿到配对好的 `(img, q, t)` | 打印帧数/时间戳，`ros2 bag play` 时你的节点在消费，图像与四元数数量对得上 |
| **M3 检测出装甲板** | 图像送进 detector，画出识别框 | 可视化窗口稳定框住装甲板、分类正确、误检少 |
| **M4 解算 + 跟踪出目标** | 跑通 solver + tracker，得到世界系目标位置/速度 | 目标位置合理，tracker 在 detecting↔tracking 间正常切换，不乱丢 |
| **M5 PlotJuggler 调试** | 把关键量发成话题，PlotJuggler 画曲线 | 曲线连续合理；**运动云台下世界系目标不系统性漂移**（坐标变换 + 时间戳对齐正确）|

**典型调试循环：**

```bash
# 终端 1：起你的自瞄节点（订阅 /image_raw + /imu/quaternion）
source install/setup.bash
ros2 run <你的包> <你的节点> <config>

# 终端 2：回放 bag（数据喂给你的节点）
ros2 bag play bags/move_translate_bag
#   反复调试同一段：加 --loop 循环播放
#   算法慢跟不上：  加 --rate 0.3 放慢

# 终端 3（M5）：看曲线
ros2 run plotjuggler plotjuggler
```

## 七、进阶项（不要求全做，有余力建议完成）

- **传统 vs 神经网络识别**：对比灯条法与 YOLO 四点检测在这段 bag 上的漏检/误检差异。
- **EKF 曲线观察**：用 PlotJuggler 看 EKF 状态随时间的曲线，判断目标运动状态有没有被稳定估计出来。

## 八、提交内容

**最终提交 = 一个公开 git 仓库**（你的整个 `sp_vision_ws/`，见 §4.1）。验收时对照下面三项自检，缺一不可：

### 1. 完整代码

- 改造成 ROS2 结构、能 `colcon build`、能 `ros2 bag play` 回放订阅的完整代码；
- 含你自建的 `autoaim_msgs` 包（§5.2）和填好内外参的配置文件（§5.1）。

### 2. 跑通证据（截图/录屏放进仓库，在 `FIX_RECORD.md` 里引用）

- 1 张 `colcon build` 成功的终端截图；
- M3：至少 1 张检测框稳定框住装甲板的可视化截图；
- M5：PlotJuggler 里目标 x/y/z、yaw、tracker 状态等曲线的截图；
- 一段 10~30s 的录屏，同时显示装甲板识别画面和 PlotJuggler 曲线。

### 3. 学习/开发记录 `FIX_RECORD.md`（验收核心）

- 按 §4.3 的固定结构写：**仓库简介 / 里程碑记录（M1–M5）/ 项目说明**；
- 做了哪些进阶项（§七）、看到什么现象，一并记进"里程碑记录"或单开一节。

> ⚠️ 截图和录屏是"跑通证据"，但**不能替代文字说明**——`FIX_RECORD.md` 里要讲清怎么复现、每一步怎么验证、遇到的问题怎么解决。见 §4.3）。

## 九、注意事项

- **先跑通，再读懂，最后才谈改。** 必做项不需要重写算法，重点是数据入口的改造。
- **读代码抓主线。** 从 `src/standard.cpp` 主循环入手，先搞清"谁调用谁、传的是什么"。
- **常见坑**：
  - 时间戳当日期用——它只是单调时钟纳秒，只用于配对和算 dt。
  - 图像和四元数没配对——各订各的、各跑各的，静止 bag 看不出，运动 bag 必露馅。
  - 内参用错镜头档——距离系统性偏大/偏小；一定用 6mm。
  - QoS 不匹配收不到消息——bag 录制用 reliable QoS，订阅端 QoS 要兼容。
  - 回放太快跟不上——用 `--rate 0.3` 放慢。
  - 世界系目标乱跳——多半是外参错、`gimbal_to_imu` 方向错，或时间戳没对齐。
- **遇到卡点先自己查、记录排查过程**，这比直接问答案更重要，也是验收看重的。

参考项目：github.com/TongjiSuperPower/sp_vision_25 ｜ 本轮练习全程离线，无需硬件
