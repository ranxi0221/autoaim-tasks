# 自瞄学习笔记（小白友好版）

> 面向对象：刚接触 RoboMaster 视觉自瞄的新手。本文不用一句公式把人劝退，先用生活类比讲直觉，再落到本项目（`sp_vision_ws`）的真实代码和数据上。
> 配套阅读：任务书 [暑期自瞄练习项目.md](暑期自瞄练习项目.md)、开发记录 [FIX_RECORD.md](FIX_RECORD.md)、环境/工具向的 [LEARNING_GUIDE.md](LEARNING_GUIDE.md)。本文只讲**自瞄算法本身**。

---

## 〇、先建立整体直觉：自瞄 = 看见 → 算出 → 打中

自瞄系统做的事，用一句话说：

> **让机器人自己完成"看见目标 → 算出目标在哪 → 控制枪口指向它 → 射击"。**

整条链路（对应原项目 [standard.cpp](src/sp_vision/src/standard.cpp) 的主循环）：

```
 相机拍照       检测装甲板        解算3D位置       跟踪+预测        算云台指令
┌─────────┐   ┌─────────┐     ┌───────────┐    ┌──────────┐    ┌──────────┐
│ 图像+时间│ → │ 找到2D框 │ →   │ 2D框→3D坐标 │ →  │ 平滑+预测 │ →  │ 枪口朝向 │ → 射击
└─────────┘   └─────────┘     └───────────┘    └──────────┘    └──────────┘
   io::Camera    detector        solver          tracker          aimer/shooter
   (M2 换成      (M3)            (M4 前半)        (M4 后半)        (真车才有)
    rosbag)
```

每步的生活类比：

| 步骤 | 算法模块 | 生活类比 |
|---|---|---|
| 检测 | `detector` | 你在人群中**认出一张脸**（还只是"屏幕上的位置"，不知道多远） |
| 解算 | `solver` | 你看到远处**一扇标准大小的窗户**，根据它在视野里的尺寸，估出你离它多远 |
| 跟踪 | `tracker` | 目标晃来晃去、偶尔被挡住，你的**眼睛和大脑会"脑补"**它一直在哪，不会一眨眼就忘了 |
| 预测 | `aimer` | 打飞碟时**瞄它的前方**，而不是它现在的位置 |

记住这条主线后再往下读，每一节都是把其中一格展开讲。

---

## 一、认识目标：装甲板（Armor）长什么样

RoboMaster 机器人身上安装的**装甲板**是自瞄的目标。它的结构很有规律，这是所有算法能成立的前提：

```
   ┌─┐                                        ┌─┐
   │█│  ← 灯条（lightbar，细长矩形，LED发光）   │█│
   │█│                                        │█│
   │█│     装甲板面板（中间）                   │█│  ← 大板宽 230mm，小板宽 135mm
   │█│                                        │█│     灯条统一长 56mm
   │█│                                        │█│
   └─┘                                        └─┘
  左灯条                                      右灯条
```

三个关键特征：

1. **两侧灯条**：细长、发亮，颜色代表阵营（红方/蓝方）。灯条发亮是为了让视觉系统在远处也能可靠找到它。
2. **尺寸固定**：大板 230mm×56mm、小板 135mm×56mm（见 [solver.cpp:12-14](src/sp_vision/tasks/auto_aim/solver.cpp#L12-L14)）。**"尺寸固定"极其重要**——这是后面 PnP 反推距离的已知条件，相当于给你一把"已知长度的尺子"。
3. **编号**：一块板上可能有多块装甲板（one/two/three…），机器人移动时不同板会轮番朝向相机，这就是你们记录里"换板瞬态跳变"的来源。

装甲板在代码里的定义见 [armor.hpp](src/sp_vision/tasks/auto_aim/armor.hpp)：`Lightbar`（灯条）+ `Armor`（由左右两条灯条 + 四个角点组成），带颜色（`Color`）、大小（`ArmorType`）、编号（`ArmorName`）、优先级（`ArmorPriority`）。

**给新手的提示**：看懂数据结构就是看懂算法的一半。`Armor` 里最关键的字段是 `points`（图像里 4 个角点的 2D 像素坐标）和 `xyz_in_world`（解算出的 3D 世界坐标）——前者是检测给解算的输入，后者是解算给跟踪的输出。

---

## 二、第一步：检测（Detect）—— 机器怎么"看见"装甲板

### 2.1 图像对计算机来说是什么

一张 1280×1024 的彩色照片，对计算机只是一个 **1280×1024×3 的数字矩阵**（3 = 蓝绿红三个通道，每个像素 0~255）。"看见目标" = 在这个矩阵里找出"哪里长得像装甲板"。

### 2.2 两条技术路线：传统灯条法 vs 神经网络

**传统灯条法**（本项目里的 `use_traditional` 选项）：

1. 按颜色阈值抠出红/蓝色像素（二值化）；
2. 找细长轮廓 = 灯条候选；
3. 把左右两根"配对"成一块装甲板，按几何关系（平行、高度接近、间距合理）筛选。

优点：不依赖训练数据、快。缺点：光照一变参数全要重调，很脆弱。

**神经网络法**（本项目实际用的，YOLO 系列）：

YOLO = You Only Look Once（你只看一眼）。把整张图一次性喂进一个卷积神经网络，它直接吐出：**每个目标的包围框（bbox）+ 类别 + 置信度 + 4 个角点**。

本项目用的是 **yolo11 四点检测版**（见 [yolo11.cpp](src/sp_vision/tasks/auto_aim/yolos/yolo11.cpp)）：输出里除了框，还直接给出装甲板的 **4 个关键点（角点）**——省掉了传统方法里"拟合矩形"这一步，四个角点直接就是 PnP 解算要用的 `armor.points`。推理引擎用 OpenVINO（CPU 上约 31~34 fps）。

**类比**：YOLO 像一个人脸打卡机——你站到摄像头前，它一帧就告诉你"框在哪、是谁、像不像"。但注意：它告诉你的只是**屏幕上的像素位置**，不是"离你 3 米远"这种真实距离。距离要靠后面两节。

### 2.3 分类器（Classifier）：知道框住了"谁"

光有框还不够，比赛规则要求打**特定的板**。分类器负责回答：

- **颜色**：red / blue（本方还是敌方）；
- **大小**：big / small（大板/小板，PvP 目标是大板）；
- **编号**：one / two / three…（哪一块板）。

你们 M3 的验收里，2982 次检测**分类 100% 一致为 `blue/one/big`**，就是说分类器在整段 bag 里没有"认错人"。

> ⚠️ 你们的真实踩坑（M4 问题 1）：`enemy_color` 配成 red 时，tracker 会把所有蓝方装甲板当"非敌方"过滤掉，结果**永远 lost，而且不报错**。教训：视觉链路里很多"静默失败"，现象是"没目标"，根因可能只是一个配置。

---

## 三、第二步：相机模型（Camera Model）—— 像素和真实世界之间的换算表

### 3.1 针孔成像：最简单的相机模型

相机成像可以用一个**小孔 + 屏幕**来近似：

```
        真实世界                    相机
     （装甲板角点P）               （成像面）
          ●                             
           \                            ┌──────────┐
            \   光线穿过小孔(光心)       │      · p │  ← P 在照片上的像素位置
             \  ──────────────────►    │    O     │
              \                        └──────────┘
                                         O = 光心
```

关键几何事实：**目标在照片上的位置和大小，取决于它与相机的距离和角度**。同一块装甲板，离得近照片上就大，离得远就小。这带来两个推论：

- 推论 1：光看照片，**距离信息丢了**（一张照片说不出"近处的小物体还是远处的大物体"）。
- 推论 2（反转过来）：如果**知道目标的真实尺寸**，再量出它在照片里的尺寸，就能**反推距离**！——这就是 PnP 的全部秘密。

### 3.2 内参：把你相机独有的"换算表"记下来

每个相机镜头都不一样，需要**标定（calibration）**得出它自己的参数：

| 参数 | 含义 | 本项目 6mm 档数值（任务书 §5.1） |
|---|---|---|
| `fx, fy` | 焦距（像素单位）：光线从光心到成像面的距离 | ≈1291.2, 1291.7 |
| `cx, cy` | 主点：光心在照片上的像素坐标（通常≈图像中心） | ≈622.5, 497.4 |
| `k1,k2,p1,p2,k3` | 畸变系数：镜头"拍歪了"的修正量 | 见任务书 |

合起来叫**内参矩阵 K**。它在代码里就是 [solver.cpp:38-39](src/sp_vision/tasks/auto_aim/solver.cpp#L38-L39) 读的 `camera_matrix` 和 `distort_coeffs`。

**畸变**是什么：广角镜头拍直线会变弯（桶形畸变）。不修正的话，用照片里的角点算位置会算偏。所以 PnP 前先用畸变系数把像素"掰直"。

> ⚠️ 你们的真实踩坑（任务书 §九）：**内参用错镜头档，距离会系统性偏大/偏小**。因为焦距 fx 直接参与距离换算，差 10% 的焦距 → 距离差 10%。你们 M5 验收里 z 轴 σ 只有 12mm，说明 6mm 档内参填对了。

### 3.3 一句话总结

相机模型 = **像素 ↔ 光线方向** 的换算表。有了它，照片里的一个像素点可以还原成空间里的一条射线；但射线上的哪个点才是目标？——需要"目标真实尺寸"这个额外条件，这就是下一步 PnP。

---

## 四、第三步：PnP 解算（solvePnP）—— 从 2D 框算出 3D 位置

### 4.1 生活类比：用 A4 纸估距离

桌上有一张标准 A4 纸（297mm×210mm，尺寸**已知**）。你拍张照片，量出照片里纸的四个角坐标。问：纸离你多远、倾斜多少度？

答案是**唯一可解**的：真实角点（3D，已知）↔ 照片角点（2D，量得），通过相机换算表，反推出相机（=你）相对纸的位置和朝向。

**这就是 PnP**：Perspective-n-Point（透视 n 点）。n 就是已知点对的数量，这里 n=4（装甲板四个角点）。

### 4.2 输入与输出

看 [solver.cpp:55-79](src/sp_vision/tasks/auto_aim/solver.cpp#L55-L79) 的 `Solver::solve()`：

**输入**：
- `object_points`：装甲板 4 个角点的**真实 3D 坐标**（把板中心当原点，大板 230×56mm 的矩形，见 solver.cpp 开头的 `BIG_ARMOR_POINTS`）；
- `armor.points`：这 4 个角点在**照片里的 2D 像素坐标**（检测器给的）；
- `camera_matrix` / `distort_coeffs`：第三节的相机换算表。

**输出**：
- `tvec`：**平移向量** = 装甲板中心在相机坐标系里的位置（"它在我右前方 1.5m"）；
- `rvec`：**旋转向量** = 装甲板相对相机的朝向（"它正对着我还是斜着"）。

```cpp
cv::solvePnP(object_points, armor.points, camera_matrix_, distort_coeffs_,
             rvec, tvec, false, cv::SOLVEPNP_IPPE);
```

算法用 **IPPE**（求解平面目标的专用快速算法）：装甲板是平面上的 4 个点，IPPE 能算出**两个候选解**，再通过"板面必须朝向相机"排除掉背面的假解。这就是为什么 4 个平面点就够：它解的是一组几何方程。

### 4.3 PnP 结果的弱点（引出下一步）

PnP 给的位置**每一帧都是独立算的**，有三个毛病：

1. **有噪声**：检测角点差 1 个像素，位置就抖一下（你们 M5 曲线里 armor_x/y/z 绕 target 摆动 ±0.2m，就是它的噪声）；
2. **会漏**：目标被挡住/转过去时，这一帧直接没有输出；
3. **没有速度**：只知道"在哪"，不知道"往哪动、多快"。

所以需要**跟踪器**把一串 PnP 结果"串起来、抹平、补全"。这就是第五节。

---

## 五、坐标变换（Coordinate Transform）—— 把"相机看到的"翻译成"世界里的"

### 5.1 为什么需要翻译：相机是装在会动的云台上的

PnP 算出的位置是**相机坐标系**里的（"目标在我镜头前方 1.5m"）。但云台在不停转动（你们的 bag 就是"运动云台"场景），同一个静止目标，云台一转，相机坐标全变。所以必须把它翻译到**世界坐标系**（"目标在场地东北角 1.46m 处"）——这个坐标不随云台转动而变。

坐标系链条：

```
相机坐标系 ──(手眼标定的外参 R_camera2gimbal, t_camera2gimbal)──► 云台坐标系
                                                                      │
世界坐标系 ◄──(IMU 姿态四元数 q，即 /imu/quaternion)──────────────────┘
```

见 [solver.cpp:65-68](src/sp_vision/tasks/auto_aim/solver.cpp#L65-L68)：

```cpp
armor.xyz_in_gimbal = R_camera2gimbal_ * xyz_in_camera + t_camera2gimbal_;  // 相机→云台
armor.xyz_in_world  = R_gimbal2world_ * armor.xyz_in_gimbal;                // 云台→世界
```

### 5.2 两个数学工具：旋转矩阵 & 四元数

**旋转矩阵 R**（3×3）：记录"坐标系 A 的三个轴在坐标系 B 里朝哪"。把坐标乘上它，就完成一次旋转翻译。

**四元数 q**（4 个数 w,x,y,z）：另一种表示旋转的方法，等价于"绕某个轴转某个角度"。为什么 IMU 要用它？因为旋转矩阵 9 个数、欧拉角 3 个数，而四元数 4 个数：**没有万向节锁**（欧拉角的姿态奇点）、**数值稳定**、**只存一个就能完整表示姿态**。你们 bag 里的 `/imu/quaternion` 就是一个四元数（w≈0.981，虚部都很小 → 云台接近水平、轻微倾斜）。

**小白记忆法**：`w` 记"转了多少"（w 越接近 1 转得越少），`x,y,z` 记"绕哪转"。

### 5.3 最容易错的一步：IMU 安装外参（sandwich 公式）

IMU 是**歪着装在车上**的，它报的"水平"不等于云台的"水平"。所以拿到 IMU 姿态后，不能直接当云台姿态用，要做一次"夹心"变换（[solver.cpp:48-52](src/sp_vision/tasks/auto_aim/solver.cpp#L48-L52)）：

```cpp
void Solver::set_R_gimbal2world(const Eigen::Quaterniond & q)
{
  Eigen::Matrix3d R_imubody2imuabs = q.toRotationMatrix();   // IMU 报的：imu体→绝对世界
  // 夹心变换：先把 IMU 姿态"转回"云台坐标系
  R_gimbal2world_ = R_gimbal2imubody_.transpose() * R_imubody2imuabs * R_gimbal2imubody_;
}
```

读法：`R_gimbal2imubody` 是"云台→IMU 的固定安装外参"（你们配置里那个全是 0/±1 的矩阵），两边一乘，就得到"云台→世界"。

> ⚠️ 你们 M5 的核心验收点"**运动云台下世界系目标不漂移**"，考的就是这一整节：外参错、夹心方向错、时间戳没对齐，任何一环都会让世界坐标随云台摆动而系统性漂移。你们的结果（线性回归斜率 0.12mm/帧、gimbal_yaw 扫过 35° 而位置纹丝不动）说明这环做对了。

---

## 六、跟踪（Tracker）：状态机 + EKF —— 让测量"稳下来、能预测"

### 6.1 为什么不能直接用 PnP 结果

第四节说过：PnP 每帧独立、有噪声、会漏检、没速度。直接拿去控制云台，枪口会**抽搐**。跟踪器干两件事：

1. **状态机**：管理"现在有没有目标"这个宏观状态，避免一帧漏检就失忆；
2. **EKF（扩展卡尔曼滤波）**：把一串带噪声的测量**融合**成一条平滑的轨迹，并给出**速度**。

### 6.2 状态机：雷达的"锁定"逻辑

跟踪器的状态机（[tracker.cpp](src/sp_vision/tasks/auto_aim/tracker.cpp) 的 `state_machine()`，你们配置 `min_detect_count=5`、`max_temp_lost_count=15`）：

```
             连续 5 帧检测到                    丢失 15 帧
  ┌────────┐ ──────────────────► ┌──────────┐ ─────────────► ┌──────┐
  │detecting│                    │ tracking │                │ lost │
  │(搜索中) │ ◄────────────────── │ (锁定中) │  ◄──────────── │(无目标)│
  └────────┘  丢1帧但未满15帧      └──────────┘  短暂丢失      └──────┘
                                      │
                              临时丢失 = temp_lost
```

- **detecting**：看到目标了，但连续命中不满 5 帧，不轻易"下单"（防止误检一晃而过）；
- **tracking**：稳定锁定，持续输出目标；
- **temp_lost**：突然 1 帧没看到（被挡/眨眼），先用预测值撑着，撑满 15 帧还没回来才算 lost；
- **lost**：彻底放弃，回到 detecting 重新搜索。

**类比**：雷达锁定敌机——发现亮点先跟踪观察几秒（detecting），锁定后短暂丢失信号也会按航迹预测（temp_lost），只有长时间消失才放弃锁定（lost）。

你们 M4 验收里状态分布 tracking 433 / detecting 89 / temp_lost 68 / lost 56 帧，切换时机与配置**逐帧吻合**，说明状态机工作正常。

### 6.3 EKF：预测 + 测量的加权平均

**先讲普通卡尔曼滤波的直觉**（两把尺子的故事）：

> 你要测桌子的长度。钢卷尺说 100.0cm，误差 ±0.5；皮尺说 101.5cm，误差 ±3。两把尺子你信哪个？
> 答案是**按误差大小加权平均**：结果 ≈ 100.1cm，更接近钢卷尺。而且两把尺子一结合，估计精度**比任何一把尺子单独用都高**。

卡尔曼滤波就是持续做这件事的数学机器：

- **预测（predict）**：按上一时刻状态 + 运动模型，猜"现在它应该在哪"（比如"上一帧在 1.46m，速度 0.5m/s，过了 0.02s，现在应该在 1.47m"）；
- **更新（update）**：新的 PnP 测量来了，按"预测的不确定度 vs 测量的不确定度"加权融合，得到新的估计。

**E 是 Extended**：运动模型是非线性的（目标绕车旋转、yaw 会周期性变），所以用扩展版（对非线性函数做一阶线性化）。

### 6.4 状态向量里装了什么

你们项目的 EKF 状态是 **11 维**（[target.hpp](src/sp_vision/tasks/auto_aim/target.hpp) 构造里的 `x0`）：

```
[x, vx, y, vy, z, vz, angle, w, r, l, h]
 │   │      │      │    │    │  │  │  │
 │   └x速度 │      └z速度│    └绕中心转角  │  │  └高度
 └x位置    └y位置       └朝向角    └转角速度 └旋转半径
```

前 6 维 = 位置 + 速度（你们 M4/M5 验收的 pos/vel 就取这 6 维）；后 5 维描述"目标绕旋转中心转圈"的运动学（角速度、半径等）——这是 RoboMaster 目标运动的特点（机器人边平移边旋转，装甲板会绕车中心转）。

**这就是为什么跟踪器能"预测"**：它不光知道目标在哪，还知道目标**往哪去、转多快**。云台指令可以按"目标 0.1 秒后的位置"来算。

### 6.5 滤波器的"体检"：发散检测

滤波器也不是永远靠谱。你们 tracker 里有两个体检项（[tracker.cpp:76-90](src/sp_vision/tasks/auto_aim/tracker.cpp#L76-L90)）：

- **diverged（发散）**：估计值跑到离谱的地方去了（比如位置突然飙到 100m）→ 直接判 lost，重新来过；
- **NIS 连续失败**：连续多次"测量值 vs 预测值差得离谱"（模型和现实对不上了）→ 判 lost。

另外 `dt > 0.1s`（两帧隔太久，说明相机卡了）也会强制 lost。你们 M4 记录"CPU 推理掉帧 → 个别 dt>0.1s 帧触发一次重置但立即恢复 detecting"，就是这个机制在工作。

---

## 七、瞄准与预测（Aimer）—— 打"未来的位置"

前六节产出的东西：**世界坐标系下，目标当前的位置和速度**（你们项目发布在 `/target/state` 上）。

真车还差最后一步（原项目 [aimer.hpp](src/sp_vision/tasks/auto_aim/aimer.hpp) / `shooter`，你们离线练习没跑）：

1. **算提前量**：子弹从出膛到命中要飞一段时间（弹速 ~15m/s，目标 5m 外 → 飞行 ~0.3s）。0.3 秒里目标会移动——所以要瞄**目标 0.3 秒后的位置**，而不是现在的位置；
2. **算云台角度**：把"未来目标位置"换算成云台的 yaw/pitch 角指令；
3. **决策**：目标进入最佳射程、姿态稳定时才开火（`choose_aim_point` 里还有"目标靠近/远离时的不同瞄点选择"）。

**类比**：打飞碟的老手瞄的不是飞碟本身，而是它飞到最高点的那片天空。

你们练习为什么停在第六节：任务书的要求是"检测 → 解算 → 跟踪 + PlotJuggler 验证"，aimer 需要下位机（`cboard`）联调，家里没有硬件。但你们发布的 `/target/state`（位置 + 速度 + yaw + 角速度 + 半径）**已经包含 aimer 所需的全部信息**。

---

## 八、全链路串讲：一帧数据的旅程

把前七节串起来，跟着你们自己的数据走一遍（基于 M4/M5 验收）：

```
1. rosbag 播出一帧
   图像 1280×1024 BGR8          四元数 q=(0.981, 0.172, -0.028, -0.088)
        │                            │
        │ 2. YOLO11 检测 (OpenVINO CPU)  │ 4. set_R_gimbal2world(q)
        ▼                            ▼   （sandwich 夹心变换）
   一块 blue/one/big 装甲板       R_gimbal2world 就绪
   + 4 个角点像素坐标
        │
        │ 3. solvePnP (IPPE)：角点 3D 模型 + 相机内参
        ▼
   相机系位置 → ×R_camera2gimbal → 云台系 → ×R_gimbal2world → 世界系
        │
        │ 5. tracker.track()：状态机筛选（enemy_color=blue）+ EKF 融合
        ▼
   /target/state 发布：pos=(1.46, -0.05, -0.31)  vel≈(0,0,0)  yaw=-0.04  state=tracking
        │
        │ 6. （真车）aimer 算提前量 → cboard 控制云台 → 开火
```

**验收数字怎么读**（M5）：

- 目标静止场景下 `pos≈(1.46, -0.05, -0.31)` 且速度≈0 → 解算和坐标变换正确（目标确实在相机前方 1.46m）；
- 云台 yaw 扫过 35°（-0.44~0.17 rad）期间，x/y/z 线性回归斜率只有 0.12mm/帧 → **世界系目标不随云台运动漂移** → 四元数使用、夹心变换、时间戳对齐全部正确；
- target（EKF 输出）平滑稳定，armor（PnP 直测）绕它摆动 ±0.2m → 直观看到 **EKF 的降噪作用**。

**哪一环错了会怎样**（对照你们的真实踩坑）：

| 出错环节 | 现象 | 你们的记录 |
|---|---|---|
| `enemy_color` 配错 | 永远没有目标、状态卡在 lost，无报错 | M4 问题 1 |
| 内参用错镜头档 | 距离系统性偏大/偏小 | 任务书 §九 |
| 时间戳没对齐/外参错 | 静止 bag 看不出，运动时世界系目标漂移 | 任务书 §九 |
| QoS 不匹配 | 根本收不到消息 | M2/M3 |
| CPU 推理掉帧 | dt>0.1s → tracker 强制重置一次 | M4 问题 3 |

---

## 九、进阶方向（有余力再看）

- **打符（auto_buff）**：打"大风车"能量机关，目标以固定角速度匀速旋转，需要先**拟合正弦轨迹**预测命中时机（代码见 `tasks/auto_buff/`，工具 `ransac_sine_fitter` 就是干这个的）；
- **MPC 云台控制**：`tasks/auto_aim/planner/tinympc/` —— 比 PID 更"会规划"的控制算法，考虑云台限位和最优轨迹；
- **GPU 推理**：你们已知局限之一（OpenVINO GPU 插件不可用，CPU 31~34fps 跟不上 46.5fps 回放）；
- **哨兵/无人机**：`sentry.cpp`、`uav.cpp` 是同一套算法在不同平台上的主程序变体。

---

## 十、术语速查表

| 术语 | 一句话解释 | 在哪 |
|---|---|---|
| Armor | 装甲板（两个灯条 + 面板，尺寸固定） | [armor.hpp](src/sp_vision/tasks/auto_aim/armor.hpp) |
| YOLO | 一阶段神经网络检测器，一次前向出框+类别+角点 | [yolo11.cpp](src/sp_vision/tasks/auto_aim/yolos/yolo11.cpp) |
| 内参 K / 畸变 | 相机"换算表"：像素↔光线方向 | `configs/standard3.yaml` |
| PnP / solvePnP | 已知真实尺寸的 4 个角点，反推 3D 位置和朝向 | [solver.cpp:55](src/sp_vision/tasks/auto_aim/solver.cpp#L55) |
| 外参 | 相机在云台上的安装位置/朝向（手眼标定） | `R_camera2gimbal`、`t_camera2gimbal` |
| 四元数 | 4 个数表示的旋转：w≈转多少，xyz≈绕哪转 | `/imu/quaternion` |
| 夹心变换 | 把歪装的 IMU 姿态换算成云台姿态 | [solver.cpp:48-52](src/sp_vision/tasks/auto_aim/solver.cpp#L48-L52) |
| 状态机 | detecting→tracking→temp_lost→lost 的锁定逻辑 | [tracker.cpp](src/sp_vision/tasks/auto_aim/tracker.cpp) |
| EKF | 预测+测量加权融合的滤波器，输出平滑轨迹和速度 | [target.hpp](src/sp_vision/tasks/auto_aim/target.hpp)、[extended_kalman_filter.hpp](src/sp_vision/tools/extended_kalman_filter.hpp) |
| 重投影 | 把 3D 结果重新投回照片，验证解算对不对 | [solver.cpp:90](src/sp_vision/tasks/auto_aim/solver.cpp#L90)（你们 M4 的橙色框） |
| 提前量 | 瞄目标"未来"的位置而非当前位置 | [aimer.hpp](src/sp_vision/tasks/auto_aim/aimer.hpp) |

---

## 十一、给新手的学习路径建议

1. **先把主线背下来**：检测(2D) → 解算(3D) → 变换(世界系) → 跟踪(平滑+预测) → 瞄准。任何细节忘了，先回到这条线；
2. **读代码从 [standard.cpp](src/sp_vision/src/standard.cpp) 主循环开始**：7 行代码就是全部骨架（任务书 §一）；
3. **验证手段要学**：你们 M4 用"重投影与检测框重合"验证解算、M5 用"运动云台下不漂移"验证坐标变换——这两个是自瞄领域最经典的验收方法，拿到哪都能用；
4. **踩坑要记录**：你们 FIX_RECORD 里"静默失败"（enemy_color）、"环境污染"（LD_LIBRARY_PATH）、"时间戳当日期用"这些坑，换个学校、换个赛季，坑一模一样。
