# ROS2 自动生成目录
build/
install/
log/

# ROS2 运行时日志（节点默认写这里）
.ros-log/

# 大文件：数据包、视频、模型权重、压缩包一律不进 git
bags/
*.db3
*.db3.zstd
*.mp4
*.avi
*.onnx
*.zip

# 本地敏感/临时文件（不入库）
.gh-token.json
.github-ca-bundle.crt
.gh-config/
.gh-cache/

# 对内自瞄仓库（参考用，不进 git、不参与 colcon 编译）
Auto_aim/

# 运行日志
logs/
