# 摆臂自瞄实现学习指南（怎么实现的）

> 面向：想看懂"我们这套摆臂代码到底是怎么跑起来的"的人。
> 配套阅读：[自瞄学习笔记.md](自瞄学习笔记.md)（算法原理：检测/PnP/EKF 是什么）、[FIX_RECORD.md](FIX_RECORD.md)（开发过程与踩坑）、[摆臂部署与协议迁移计划.md](摆臂部署与协议迁移计划.md)（协议规格字节表）。

---

## 一、一张图看懂：从相机到摆臂的完整旅程

```
海康相机 ──150fps 1280×1024──► YOLO 检测（yolo11/yolov5，OpenVINO）
                                    │ 输出：每块装甲板的 4 个角点像素坐标（不分颜色）
                                    ▼
                              分类器（颜色 red/blue、大小、编号）
                                    │
                              跟踪器 Tracker ── enemy_color 过滤（这里只留敌人颜色！）
                                    │ 剩下的板做 PnP 解算（队内内参）→ 相机系 3D 位置
                                    ▼
                              坐标变换 Solver ── 队内外参：相机→云台→世界（用下位机四元数）
                                    │
                               EKF 跟踪：平滑 + 预测速度（11 维状态）
                                    │
                               规划器 Planner（MPC）── 算云台角度/开火决策
                                    │
                               io::Gimbal ── 打包 28 字节 ITL 帧 ──► 串口 ──► 下位机
                                                                           │
   下位机 42 字节反馈帧（四元数/云台角/弹速）◄────────────────────────────┘
```

对应代码：主循环在 [standard_mpc.cpp](src/sp_vision/src/standard_mpc.cpp)，算法在 [tasks/auto_aim/](src/sp_vision/tasks/auto_aim/)，通信在 [io/gimbal/](src/sp_vision/io/gimbal/)。

---

## 二、红蓝切换是怎么实现的（按下 `r` 之后发生了什么）

这是你昨天演示成功的功能，代码只有 3 处，但串起了一个完整的机制：

### 1. 按键从哪里来

主循环每帧末尾有 [standard_mpc.cpp:185-195](src/sp_vision/src/standard_mpc.cpp#L185-L195)：

```cpp
auto key = cv::waitKey(1);          // ① 泵送窗口刷新事件（没有它窗口只闪第一帧）
if (key == 'r' && enemy_color != auto_aim::Color::red) {
  enemy_color = auto_aim::Color::red;          // ② 改局部变量
  tracker.set_enemy_color(enemy_color);        // ③ 同步给跟踪器
  tools::logger()->info("[AutoAim] 敌方颜色切换为 red（按键 r=红 / b=蓝）");
}
```

- `cv::waitKey(1)` 干两件事：**刷新 OpenCV 窗口**（我们之前修好的 bug）+ **返回这一帧按下的键**（ASCII 码）。按 `r` 就返回 114（'r'）。
- 注意按键只在 **detection 窗口有焦点**时才收得到——所以要先鼠标点一下窗口。

### 2. 颜色变量存哪

[standard_mpc.cpp:84](src/sp_vision/src/standard_mpc.cpp#L84)：启动时从 `arm_deploy.yaml` 的 `enemy_color` 读默认值，之后按键改它。它只用于**显示**（状态日志里的 `color=`）——真正的过滤逻辑在跟踪器里。

### 3. 过滤在哪生效

[tracker.cpp:43](src/sp_vision/tasks/auto_aim/tracker.cpp#L43)，每次 `track()` 的第一件事：

```cpp
armors.remove_if([&](const auto_aim::Armor & a) { return a.color != enemy_color_; });
```

把"不是敌人颜色"的装甲板**从列表里删掉**，剩下才去做解算和跟踪。`set_enemy_color()`（[tracker.hpp:24-26](src/sp_vision/tasks/auto_aim/tracker.hpp#L24-L26)）就是改 `enemy_color_`，下一帧立即生效——**所以切换后最多一帧（~7ms）就换了目标**，这就是"来回切换很顺滑"的原因。

### 4. 为什么之前"红色识别不稳定"

这就是最经典的坑：**检测正常，跟踪被过滤**。

- YOLO **不分颜色**——红板蓝板都会画框（所以窗口里两个颜色的框都有）
- 跟踪器按 `enemy_color` 过滤——当时配置是 `blue`，红板永远在过滤那一步被删掉
- 症状：窗口有框，但 `[AutoAim]` 日志里 `armors=1 targets=0 state=lost`

判别口诀：`armors=1`（检测到了）但 `targets=0`（跟踪不到）→ **大概率是颜色过滤，不是算法坏了**。

---

## 三、和电控的协议是怎么对上的（字节级）

电控固件一行没改，我们的帧必须和队内 ITL 视觉逐字节一样。实现在 [io/gimbal/gimbal.hpp](src/sp_vision/io/gimbal/gimbal.hpp)：

```cpp
// 发往电控：28 字节（static_assert 保证）
struct __attribute__((packed)) VisionToGimbal {
  uint8_t head[2] = {'V','G'};   // 帧头
  uint8_t mode;                  // 0 不控制 / 1 控制不开火 / 2 控制+开火
  float yaw, yaw_vel, yaw_acc;   // 绝对角(rad) + 前馈速度/加速度
  float pitch, pitch_vel, pitch_acc;
  uint8_t tail = 'V';            // 帧尾
};

// 电控发来：42 字节
struct __attribute__((packed)) GimbalToVision {
  uint8_t head[2] = {'G','V'};
  uint8_t mode;                  // 0 空闲 / 1 自瞄 / 2 小符 / 3 大符
  float q[4];                    // 板载 IMU 四元数（wxyz）
  float yaw, yaw_vel, pitch, pitch_vel;  // 云台实际角/角速度
  float bullet_speed;
  uint16_t bullet_count;
  uint8_t tail = 'G';
};
```

**设计要点**（为什么这样设计就兼容了）：

1. **定长帧 + 无 CRC**：靠 `static_assert(28/42 字节)` 编译期锁死长度，收帧只查头尾字节。浮点数里可能恰好出现 `'G','V'` 字节序列，所以还要配合逐字节扫描重同步（[gimbal.cpp](src/sp_vision/io/gimbal/gimbal.cpp) 的 `read_thread`）。
2. **打包结构体直接收发**：`reinterpret_cast` 整个结构体写串口——C++ 内存布局 = 线上字节序（x86/ARM 都是小端，和 STM32 一致）。
3. **四元数时间对齐**：收帧线程把（四元数, 时间戳）push 进队列；主循环用相机帧的时间戳调 `gimbal.q(t)` 做 **slerp 插值**，拿到"这一帧图像拍摄瞬间"的云台姿态。
4. **q_calib**：每台车 IMU 安装角度不同，`q_corrected = q_calib * q` 在校准入队前完成。
5. **断线重连**：读错误 >100 次自动关串口重开（下位机掉电再上电能自愈）。

完整字节表见[摆臂部署与协议迁移计划.md](摆臂部署与协议迁移计划.md)第三节。

---

## 四、标定参数从哪来、用在哪

摆臂用的是**队内仓库的标准值**（`Auto_aim/src/config/standard3.yaml` 顶部「老步兵——摆臂」一节），已填入 [arm_deploy.yaml](src/sp_vision/configs/arm_deploy.yaml)：

| 参数 | 值（6mm 档） | 用在哪 |
|---|---|---|
| 内参 fx/fy, cx/cy | 1330.6 / 1332.3, 627.3 / 533.3 | PnP：像素→3D 位置（[solver.cpp](src/sp_vision/tasks/auto_aim/solver.cpp) `solve()`） |
| 畸变系数 k1~k3 | 队内 5 个值 | PnP 前修正镜头畸变 |
| R_camera2gimbal | [0,0,1; -1,0,0; 0,-1,0] | 相机系→云台系（左手系→右手系的坐标轴置换） |
| t_camera2gimbal | [0.1519, 0.0756, 0.0222] m | 相机在云台上的安装偏移 |
| R_gimbal2imubody | [0,1,0; -1,0,0; 0,0,1] | IMU 四元数的"夹心变换"（[solver.cpp:48-52](src/sp_vision/tasks/auto_aim/solver.cpp#L48-L52)） |
| q_calib | 单位四元数（暂不需要） | 收帧时校正 IMU 安装角 |

**验证方法**（你们已经做过的）：把装甲板放 1.5m 处，日志 `pos=(1.5, ...)m` 与实际距离吻合 → 标定正确。如果 pos 是几十米 → 内参或外参错了。

---

## 五、配置导览：调参改哪（[arm_deploy.yaml](src/sp_vision/configs/arm_deploy.yaml)）

| 键 | 谁读它 | 干什么 |
|---|---|---|
| `camera_name` / `vid_pid` | [camera.cpp](src/sp_vision/io/camera.cpp) 工厂 | 选海康/迈德威视 |
| `exposure_ms` / `gain` | 相机驱动 | 画面亮度（红色灯条过曝就降 exposure） |
| `image_width/height` | 相机驱动 | 输出分辨率（**必须 1280×1024，内参按此标定**） |
| `enemy_color` | tracker + standard_mpc | 默认敌方颜色（运行时可 r/b 覆盖） |
| `com_port` / `baudrate` | [gimbal.cpp](src/sp_vision/io/gimbal/gimbal.cpp) | 串口设备/波特率（115200 勿改） |
| `force_mode` | standard_mpc | 摆臂恒发 mode=0，用它强制进自瞄档（真车删掉） |
| `first/second_tolerance` | standard_mpc | 开火门容差（指令角 vs 反馈角） |
| `min_detect_count` 等 | tracker | 状态机阈值 |

---

## 六、摆臂专属改动清单（相对 sp25 原版，我们改了什么）

| 改动 | 文件 | 为什么 |
|---|---|---|
| ITL 协议（头尾/115200/q_calib） | io/gimbal/ | 电控固件只认这套帧 |
| `force_mode` 强制自瞄档 | standard_mpc.cpp | 摆臂下位机没有档位切换 |
| 键盘 r/b 切换敌方颜色 | standard_mpc.cpp + tracker.hpp | 演示红蓝来回切 |
| 每秒状态日志（含颜色/位置） | standard_mpc.cpp | 无 GUI 也能验收 |
| `cv::waitKey(1)` | standard_mpc.cpp | 修窗口只闪一帧不刷新 |
| 相机定 1280×1024 + 可配帧率 | hikrobot/mindvision | 原 1440×1080 与标定内参不匹配 |
| 无目标回显当前角 | standard_mpc.cpp | 对齐队内行为，避免 mode 切换阶跃 |
| 开火门控（cmd vs 反馈角） | standard_mpc.cpp | 降低空枪率，auto_fire 可关 |
| start_arm.sh / watchdog.sh | scripts/ | 一键启动 / 崩溃自愈 |

---

## 七、常见现象的代码级解释

| 现象 | 代码原因 | 一句话 |
|---|---|---|
| 窗口只闪一帧 | 缺 `cv::waitKey` | OpenCV 窗口靠它泵送刷新事件 |
| 启动卡在 `First q received` 之前 | Gimbal 构造函数阻塞等第一帧（gimbal.cpp 构造里 `queue_.pop()`） | 先给下位机上电再启程序 |
| `Too many errors` 刷屏 | 波特率不匹配/线松 | 115200 是硬约定 |
| 红板有框但 `targets=0` | tracker 的 `remove_if` 颜色过滤 | 按 `r` 或改 enemy_color |
| 红板检测框闪烁 | 红色灯条过曝，分类器颜色特征被洗掉 | 降 exposure_ms 到 2.5/2.0 |
| pos 几十米 | 内参/外参与实际相机不符 | 重标定或核对队内值 |
| 摆臂只动一根轴但 yaw/pitch 都变 | 两轴机械联动，固件如实上报 | 不是 bug |
| `[AutoAim]` 无输出 | 不在 AUTO_AIM 分支（force_mode 没生效/模式不对） | 查 force_mode 键 |
