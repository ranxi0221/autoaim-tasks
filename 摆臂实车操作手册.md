# 摆臂实车操作手册（照着做就行）

> 目标：把 sp25 在摆臂上跑起来（真相机 + 真下位机 + 真云台）。
> 每步有「✅ 检查」，通过才进下一步；卡住就把**报错原文**发我。
> 需要显示器（或 NoMachine 远程桌面），标定程序要弹窗口。

---

## 第 0 步：把代码弄到摆臂电脑上

- **方式 A（推荐）**：U 盘把整个 `sp_vision_ws` 文件夹拷到摆臂电脑（比如放 `~/sp_vision_ws`）。
- **方式 B**：摆臂电脑能上 GitHub 的话，`git clone` 你的仓库（需先 push 最新代码，跟我说一声我帮你提交）。

✅ 检查：摆臂电脑上能找到 `~/sp_vision_ws/src/sp_vision/configs/arm_deploy.yaml`。

## 第 1 步：检查系统（5 分钟）

```bash
lsb_release -a          # 必须是 Ubuntu 22.04
printenv ROS_DISTRO     # 必须是 humble；空的话先执行 source /opt/ros/humble/setup.bash 再查
```

- 如果没装 ROS2：`sudo apt install ros-humble-ros-base`
- 版本不是 22.04/Humble 的话先告诉我，我帮你调安装命令。

## 第 2 步：装依赖（复制粘贴，约 20 分钟）

```bash
sudo apt-get install -y libopencv-dev libfmt-dev libeigen3-dev libspdlog-dev \
  libyaml-cpp-dev libusb-1.0-0-dev nlohmann-json3-dev libceres-dev \
  screen openssh-server can-utils

# OpenVINO（必须 Intel APT 版，pip 版会编译失败，M1 踩过坑）
wget -qO- https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB | sudo gpg --dearmor --yes -o /usr/share/keyrings/intel-sw.gpg
echo "deb [signed-by=/usr/share/keyrings/intel-sw.gpg] https://apt.repos.intel.com/openvino/2024 ubuntu22 main" | sudo tee /etc/apt/sources.list.d/intel-openvino.list
sudo apt update && sudo apt install -y openvino
```

相机 SDK 等第 3 步确认相机型号后再装。

## 第 3 步：确认硬件（先做这步）

```bash
lsusb
```

| 看到 | 说明 | 要装的 SDK |
|---|---|---|
| `2bdf:` 开头 | 海康相机 | 海康 MVS（官网下 Linux 安装包，默认装到 /opt/MVS） |
| `f622:` 开头 | 迈德威视相机 | 迈德威视官方 SDK |

再找 MCU 串口：**拔插一下下位机的 USB 线**，看哪个设备出现/消失：

```bash
ls /dev/ttyACM*
```

镜头焦距看镜头上印的字（6mm？8mm？）。

📩 **把这三个答案发我：① 相机是海康还是迈德威视 ② 镜头几 mm ③ MCU 是 /dev/ttyACM 几**。

## 第 4 步：改配置两行（1 分钟）

编辑 `~/sp_vision_ws/src/sp_vision/configs/arm_deploy.yaml`：

```yaml
camera_name: "hikrobot"     # 或 "mindvision"，按第 3 步结果
vid_pid: "2bdf:0001"        # 海康常见值；迈德威视常见 "f622:d13a"，以 lsusb 实际为准
```

## 第 5 步：编译（约 2 分钟）

```bash
cd ~/sp_vision_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install
```

✅ 检查：结尾 `Summary: 1 package finished`，没有 `Failed`。

## 第 6 步：串口权限 + 固定设备名（5 分钟）

```bash
sudo usermod -a -G dialout $USER     # 执行后注销重新登录一次才生效

# 找 MCU 串口的 ID（ttyACM0 换成第 3 步看到的实际名字）
udevadm info -a -n /dev/ttyACM0 | grep -E '({serial}|{idVendor}|{idProduct})'

# 把上面查到的三个值填进下面规则（SYMLINK 必须叫 gimbal）
sudo tee /etc/udev/rules.d/99-usb-serial.rules <<'EOF'
SUBSYSTEM=="tty", ATTRS{idVendor}=="1234", ATTRS{idProduct}=="1234", ATTRS{serial}=="A1234567", SYMLINK+="gimbal"
EOF
sudo udevadm control --reload-rules && sudo udevadm trigger
ls -l /dev/gimbal
```

✅ 检查：显示 `/dev/gimbal -> ttyACM0`（指向实际串口）。

## 第 7 步：相机验证（看到画面就过）

```bash
source ~/sp_vision_ws/install/setup.bash
ros2 run sp_vision camera_test -c=src/sp_vision/configs/arm_deploy.yaml
```

✅ 检查：弹出窗口能看到画面，分辨率 1280×1024，帧率 ≥30fps。

## 第 8 步：下位机串口验证（第一次联通）

> ⚠️ **顺序很重要：先给下位机上电，再启动程序**——程序启动时会等第一帧四元数。

```bash
source ~/sp_vision_ws/install/setup.bash
ros2 run sp_vision gimbal_test -f src/sp_vision/configs/arm_deploy.yaml
```

✅ 检查：
- 日志出现 `[Gimbal] First q received.`（收到数据）
- 电控切档位（空闲/自瞄/小符/大符），日志打印 `Gimbal mode changed: ...`
- 没有 `Too many errors` 刷屏

**顺手验证符号约定**（很重要）：相机慢慢**上仰**，反馈的 pitch 应变**负**；云台向**左**转，yaw 应变**正**。把看到的现象发我。

## 第 9 步：标定（最需要动手，约 1~2 小时）

需要的东西：**标定板**——对称圆点板，10 列 × 7 行，圆心距 40mm。

### 9.1 相机内参标定

1. 下位机上电（采集要四元数）。
2. 手拿标定板，在相机视野里变换位置和角度（远近、左右、上下倾斜都要有）。
3. 启动采集程序，**每摆好一个姿势按一下键盘 `s`** 保存一张，攒 20 张左右，按 `q` 退出：

```bash
cd ~/sp_vision_ws/src/sp_vision
source ../../install/setup.bash
ros2 run sp_vision capture_gimbal -c configs/arm_deploy.yaml -o assets/img_with_q
```

4. 算内参（会逐张弹窗显示识别结果，**每张按一下回车**继续；`failure` 的图会被跳过，成功 ≥15 张就行）：

```bash
ros2 run sp_vision calibrate_camera -c configs/calibration.yaml assets/img_with_q
```

5. 把打印出来的 `camera_matrix` / `distort_coeffs` 整体复制进 `arm_deploy.yaml`，替换占位值。

✅ 检查：打印的「重投影误差」< 0.5px。

### 9.2 手眼标定（相机→云台外参）

1. 先把 `arm_deploy.yaml` 里的 `R_gimbal2imubody` 复制到 `configs/calibration.yaml` 的对应行（保持一致）。
2. 标定板**固定不动**（贴墙上），转动云台让相机从不同角度看它。
3. 每个角度按 `s` 保存，攒 15~25 张，按 `q` 退出（命令同 9.1 第 3 步）。
4. 算手眼（同样逐张回车确认）：

```bash
ros2 run sp_vision calibrate_handeye -c configs/calibration.yaml assets/img_with_q
```

5. 把打印的 `R_camera2gimbal` / `t_camera2gimbal` 填进 `arm_deploy.yaml`。
6. 这一步画面上会显示云台角度读数：只转 yaw 时 pitch/roll 应该基本不动；如果乱跳，检查 `calibration.yaml` 的 `R_gimbal2imubody` 是否和 `arm_deploy.yaml` 一致。

### 9.3 q_calib（IMU 安装角校准）

1. 用水平仪把云台放到**物理水平**。
2. 跑 `gimbal_test` + PlotJuggler 看四元数换算出的 pitch/roll。
3. 残留的常值角度（比如 pitch 恒为 +2°）就是安装误差，把它发我，我帮你换算成四元数填进 `arm_deploy.yaml` 的 `q_calib`。

## 第 10 步：全链路（standard_mpc 三档递进）

```bash
cd ~/sp_vision_ws/src/sp_vision
source ../../install/setup.bash
ros2 run sp_vision standard_mpc configs/arm_deploy.yaml
```

1. 电控切**空闲**：日志正常、串口无报错、无 `Too many errors`。
2. 电控切**自瞄**：拿一块装甲板（或打印的装甲板图）在相机前，PlotJuggler 看目标位置和 cmd/feedback 收敛曲线。
3. 电控切**小符/大符**：有输出、不报错即可（冒烟）。

PlotJuggler 启动前先过滤海康 SDK 的环境污染（否则 Qt 闪退）：

```bash
export LD_LIBRARY_PATH=$(printf '%s' "$LD_LIBRARY_PATH" | tr ':' '\n' | grep -v '/opt/MVS' | paste -sd: -)
ros2 run plotjuggler plotjuggler
```

## 第 11 步：开机自启（可选）

把 `autostart.sh` 的**绝对路径**写进 `~/.config/autostart/sp_vision.desktop`（写法见 `src/sp_vision/readme.md`「注册自启」一节），重启后自瞄程序会在 screen 里自动跑起来（watchdog 守护，崩了自动重启）。

---

## 常见问题速查

| 现象 | 原因 / 处理 |
|---|---|
| `[Gimbal] Failed to open serial` | com_port 不对 / 下位机没上电 / 没重新登录（dialout 组） |
| 启动后卡住没日志 | 在等第一帧四元数：**先给下位机上电再启程序** |
| `Too many errors` 刷屏 | 波特率不是 115200 / 线接触不良 / 固件没跑起来 |
| 相机打不开 | camera_name / vid_pid 配错 / SDK 没装 / USB 没插好 |
| PlotJuggler 闪退 | 没过滤 /opt/MVS 的 LD_LIBRARY_PATH |
| 目标距离明显不对 | 内参还是占位值 / 输出分辨率不是 1280×1024 |
| 云台转动时目标乱飘 | 手眼外参错 / q_calib 没标 / 时间戳没对齐 |
| 标定弹窗出不来 | 没显示器：接屏幕或装 NoMachine 远程桌面 |

---

## 📩 每完成一步，把这些发我

1. 第 3 步的三个答案（相机型号 / 镜头焦距 / MCU 串口号）
2. 每一步 ✅ 检查过没过，失败的**报错原文**
3. 第 8 步符号约定验证结果（上仰 pitch 是否为负、左转 yaw 是否为正）
4. 第 9 步标定出来的数值
