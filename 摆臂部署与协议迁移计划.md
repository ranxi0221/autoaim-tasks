# 摆臂实车部署 sp25 + 迁移 ITL 通讯协议 — 实施计划（审核稿）

> 日期：2026-09-09 ｜ 状态：待审核 ｜ 实施前请电控同学重点核对第三节协议规格与第八节确认事项

---

## 一、背景与目标

`sp_vision_ws` 已完成暑期离线练习（M1~M5：sp_vision_25 的 ROS2 化 + rosbag 回放跑通 检测→解算→跟踪）。下一步进入实车阶段，两个任务：

1. **实车部署**：把 sp25 部署到摆臂实车（x86 工控机 + Ubuntu）上，跑真相机、真下位机（RM C 板 STM32F407）、真云台。
2. **协议迁移**：把现役自瞄仓库 [RM-ITL/Auto_aim](https://github.com/RM-ITL/Auto_aim)（ITL_Vision）与电控下位机的串口通讯协议迁入 sp25。**前提：电控固件一行不改** —— sp25 发出的帧必须与现役 ITL 视觉逐字节一致。

已确认前提：
- 相机：**海康 MVS 与迈德威视都支持**（配置切换）；
- 云台姿态四元数：**走对内协议**（由下位机串口回报，不使用 DM 电机 IMU）；
- 电控固件：**必须 100% 兼容现有固件**；
- 计算平台：x86 工控机 + Ubuntu。

---

## 二、现状与关键结论

### 2.1 sp25 现有的两条下位机通道

| 通道 | 类 | 物理通道 | 协议特征 | 当前使用方 |
|---|---|---|---|---|
| 旧 | `io::CBoard` | SocketCAN | 8 字节裸帧，无帧头/CRC | `standard.cpp`、sentry、uav |
| 新 | `io::Gimbal` | USB 虚拟串口 | `'S','P'` 帧头 + CRC16 打包协议 | `standard_mpc.cpp` |

### 2.2 关键发现：ITL_Vision 本身就是 sp_vision_25 的派生仓库

两个仓库同源。ITL 与我们电控固件之间的协议，和 sp25 自带的 `io::Gimbal` 串口协议**字段布局完全一致**，只有 4 处差异：

| # | 差异点 | sp25 现状 | ITL 现役协议（目标） |
|---|---|---|---|
| 1 | 帧头 | `'S','P'` | `'V','G'`（下行）/ `'G','V'`（上行） |
| 2 | 校验 | CRC16（2 字节） | 无 CRC，1 字节帧尾 `'V'`/`'G'` |
| 3 | 波特率 | 未设置（bug，实际 9600） | 115200 硬编码 |
| 4 | 四元数校准 | 无 | YAML `q_calib` 安装角校准 |

**结论：迁移工作量集中在一个文件（`io/gimbal/`），算法链路（检测/解算/跟踪/规划）零改动。**

---

## 三、目标协议规格（电控审核重点）

数据来源：RM-ITL/Auto_aim `src/io/deveice/gimbal/include/gimbal.hpp`（实施时再逐条复核源码）。

**串口参数**：115200 baud、8N1、50ms 读超时；无握手、无协议版本协商、无心跳帧；双方持续流式互发（视觉约 100Hz）；收方按帧头字节扫描重同步，连续 >100 次读错误自动重连。

### 3.1 Vision → MCU：`VisionToGimbal`（共 28 字节）

| 偏移 | 长度 | 字段 | 类型 | 含义 |
|---|---|---|---|---|
| 0-1 | 2 | head | u8×2 | `'V','G'` = 0x56 0x47 |
| 2 | 1 | mode | u8 | 0 不控制；1 控制云台不开火；2 控制云台且开火 |
| 3-6 | 4 | yaw | float32 LE | 云台 yaw **绝对目标角**（rad） |
| 7-10 | 4 | yaw_vel | float32 LE | 前馈角速度（rad/s） |
| 11-14 | 4 | yaw_acc | float32 LE | 前馈角加速度（rad/s²） |
| 15-18 | 4 | pitch | float32 LE | 云台 pitch **绝对目标角**（rad） |
| 19-22 | 4 | pitch_vel | float32 LE | 前馈角速度（rad/s） |
| 23-26 | 4 | pitch_acc | float32 LE | 前馈角加速度（rad/s²） |
| 27 | 1 | tail | u8 | `'V'` |

### 3.2 MCU → Vision：`GimbalToVision`（共 42 字节）

| 偏移 | 长度 | 字段 | 类型 | 含义 |
|---|---|---|---|---|
| 0-1 | 2 | head | u8×2 | `'G','V'` = 0x47 0x56 |
| 2 | 1 | mode | u8 | 0 空闲；1 自瞄；2 小符；3 大符 |
| 3-18 | 16 | q[4] | float32×4 LE | 板载 IMU 四元数，**wxyz 顺序** |
| 19-22 | 4 | yaw | float32 LE | 云台当前 yaw（rad，绝对） |
| 23-26 | 4 | yaw_vel | float32 LE | 云台 yaw 角速度（rad/s） |
| 27-30 | 4 | pitch | float32 LE | 云台当前 pitch（rad，绝对） |
| 31-34 | 4 | pitch_vel | float32 LE | 云台 pitch 角速度（rad/s） |
| 35-38 | 4 | bullet_speed | float32 LE | 弹速（m/s） |
| 39-40 | 2 | bullet_count | u16 LE | 子弹累计发射次数 |
| 41 | 1 | tail | u8 | `'G'` |

### 3.3 行为约定（与 ITL 一致，请电控确认）

1. **角度约定**：绝对角、弧度制；**pitch 抬头为负、低头为正；yaw 向左为正、向右为负**（sp25 aimer 输出与 ITL 同源同约定，但仍需实车复核）。
2. **无目标/空闲帧**：视觉仍按 ~100Hz 发 mode=0 帧，但**回显当前反馈角度**（yaw/pitch 取最近一次收到的 `gs.yaw/gs.pitch`，速度/加速度字段为 0），不发送全零。
3. **开火门控**：视觉侧在 `planner.fire` 基础上加一道门——指令角 vs 电控反馈角的偏差小于容差才发 mode=2（近距 <2m 容差 1.8°，远距 1.3°）。此门在视觉侧，可配置关闭。
4. **四元数校准**：视觉侧应用 `q_corrected = (q_calib · q).normalized()`，`q_calib` 为每车安装角校准四元数（YAML 配置 `[x,y,z,w]`，默认单位四元数）。电控侧无需处理。
5. **协议变体范围**：本次只做标准云台协议（步兵/摆臂）。哨兵变体（含 mode=4 扫描、底盘速度指令）与飞镖变体不在本次范围，需要时另行立项。

---

## 四、实施计划（任务二：协议迁移，开发机完成）

| 编号 | 工作项 | 内容 | 涉及文件 |
|---|---|---|---|
| W0 | 源码复核 | 实施时对照 ITL 仓库 `gimbal.hpp/gimbal.cpp/standard3.cpp` 逐条核实第三节规格 | — |
| W1 | 帧结构重定义 | 删 CRC16 字段、改头尾字节、`static_assert` 精确到 28/42 字节；类接口不变（8 个调用点零改动） | `io/gimbal/gimbal.hpp` |
| W2 | 串口参数 | 加 `setBaudrate`（YAML 键 `baudrate`，缺省 115200，修复现 9600 bug）+ 50ms 超时 | `io/gimbal/gimbal.cpp` |
| W3 | 读线程 | 逐字节扫描帧头（修复现 2 字节步进的死同步死角）、帧尾字节校验替代 CRC、错误阈值 5000→100 | `io/gimbal/gimbal.cpp` |
| W4 | q_calib | 构造读取 YAML `q_calib`（缺省单位四元数），入队前左乘校正 | `io/gimbal/gimbal.{hpp,cpp}` |
| W5 | send 去 CRC | 两个 send 重载直接写 28 字节结构体 | `io/gimbal/gimbal.cpp` |
| W6 | 无目标回显 | 主循环 IDLE 分支与退出帧改为回显当前反馈角 | `src/standard_mpc.cpp` |
| W7 | 开火门 | plan 线程加 cmd-vs-反馈角门控（容差 1.8°/1.3°，`auto_fire` 键可关） | `src/standard_mpc.cpp` |
| W8 | 新配置 | 新建 `configs/arm_deploy.yaml`（相机可切换、新键、内外参占位） | `configs/arm_deploy.yaml` |
| W9 | 离线验证 | 字节布局单测 + Python MCU 仿真器（pty）+ 端到端 | `tests/gimbal_frame_test.cpp`、`scripts/mcu_emulator.py` |

**任务二验收标准（无硬件，开发机闭环）**：
- 帧字节布局测试通过（28/42 字节精确、头尾正确、无 CRC 残留）；
- MCU 仿真器连续 10 分钟零失败，注入随机垃圾字节后重同步正常；
- IDLE 回显断言通过；mode 0/1/2/3 切换日志正确；
- q_calib 注入已知偏移后输出四元数被正确校正。

---

## 五、实施计划（任务一：摆臂实车部署）

| 阶段 | 内容 |
|---|---|
| A 系统依赖 | Ubuntu 22.04 + ROS2 Humble；apt 依赖清单（见 FIX_RECORD M1）+ screen/openssh；OpenVINO 用 Intel APT 版（ABI=1）；海康 MVS（/opt/MVS）+ 迈德威视官方 SDK；colcon build；NoMachine + PlotJuggler |
| B udev | dialout 组；udev 规则把 MCU 软链为 `/dev/gimbal` |
| C 相机支持 | hikrobot 驱动加 1280×1024 定分辨率 + 可配置 fps（现为 1440×1080 原生 + 150fps 硬编码，与 1280×1024 标定内参不匹配必须修）；mindvision 同样固定 1280×1024；`camera_test` 验证 |
| D 标定 | 新建 `calibration/capture_gimbal.cpp`（现 capture 走 CAN 取四元数，摆臂四元数走串口）；产出：内参、`R_camera2gimbal/t_camera2gimbal`、`R_gimbal2imubody` 校核、`q_calib` |
| E 部署 | 主程序 = `standard_mpc`；补缺失的 `watchdog.sh`、修 `autostart.sh` 硬编码路径；脚本显式 cd 到 `src/sp_vision` |
| F 实车验收 | 分步：相机 → 串口/mode 切换/符号约定 → 三档递进（IDLE/AUTO_AIM/BUFF）→ 断电重启自恢复演练 → 实弹命中率（有条件时） |

---

## 六、总体实施顺序

```
协议迁移 W0–W9（开发机，含仿真闭环验证）
        ↓
工控机环境 A–B → 相机驱动 C → 标定 D → 部署 E
        ↓
实车验收 F
```

---

## 七、风险与注意事项（摘要）

1. **字节布局**：packed 结构体 + 编译期字节数断言兜底；x86/ARM 均为小端，与 STM32 一致，无字节序问题。
2. **无 CRC 协议**：float 载荷中可能出现帧头字节序列，靠定长帧 + 尾字节校验 + 逐字节重同步（与 ITL 相同策略）。
3. **q_calib 乘法顺序**：必须 `q_calib · q`，写反则瞄准带安装角偏置且不易察觉（仿真器注入已知偏移验证）。
4. **波特率 bug**：现 `io::Gimbal` 未设波特率（跑在 9600），不修则对 115200 固件全是乱码 → 必须修。
5. **启动时序**：`Gimbal` 构造阻塞等第一帧，**下位机必须先于视觉程序上电**；日志 `[Gimbal] First q received.` 是排查锚点。
6. **回显行为**：IDLE 帧改为回显当前角是 ITL 行为——**请电控确认固件对 mode=0 帧的处理**（是否忽略指令、有无特殊逻辑）。
7. **fire gate 影响**：视觉侧开火门会让首发射击更保守（等实际云台收敛才开火），嫌慢可配置关闭。
8. **分辨率-内参耦合**：相机驱动必须固定 1280×1024 输出，否则实车标定的内参作废。
9. **遗留项**：tracker 选目标的图像中心硬编码 1440×1080（多目标场景选板偏置），实车前顺手修或记录。
10. **PlotJuggler**：启动前需过滤 `LD_LIBRARY_PATH` 中的 /opt/MVS（海康 SDK 自带 Qt 污染，M5 已踩坑）。

---

## 八、需要审核/确认的事项

**电控侧请确认：**
- [ ] 第三节 3.1/3.2 帧规格与固件实际解析一致（尤其字段顺序、字节序、头尾字节）；
- [ ] 角度约定（pitch 抬头为负、yaw 左正、绝对角）与固件一致；
- [ ] 固件对 mode=0 帧（含回显角度）的处理方式；
- [ ] 弹速字段范围与固件下发值（视觉侧弹道解算依赖）；
- [ ] 串口参数：115200 8N1 确认；固件上电后是否持续自发反馈帧（视觉构造依赖）。

**视觉侧待定：**
- [ ] 摆臂实车上当前安装的相机型号（海康 / 迈德威视），决定 `arm_deploy.yaml` 默认值；
- [ ] 摆臂云台的 IMU 安装姿态与现有 `R_gimbal2imubody` 是否一致（标定时校核）；
- [ ] 摆臂镜头焦距档（6mm / 8mm），影响内参标定。

---

## 九、交付物清单

1. 修改后的 `io/gimbal/`（ITL 协议，字节级兼容）；
2. `configs/arm_deploy.yaml`（摆臂部署配置模板）；
3. `tests/gimbal_frame_test.cpp`（字节布局单测）+ `scripts/mcu_emulator.py`（MCU 仿真器）；
4. 相机驱动改造（1280×1024 + 可配置 fps）+ `calibration/capture_gimbal.cpp`；
5. `watchdog.sh` + 修复后的 `autostart.sh`（开机自启）；
6. 实车验收记录（沿用 FIX_RECORD.md 结构追加）。
